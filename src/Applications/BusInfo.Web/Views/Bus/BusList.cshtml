@model List<BusInfo.Core.Classes.Bus>
@{
    Layout = "_Layout";
}
<body>
<main class="content">
    <div class="container-fluid p-0">

        <h1 class="h3 mb-3">Рӯйхати нақлиёт</h1>

        <div class="row">
            <div class="col-12">
                <div class="card">
                    <div class="card-header">

                        <div class="">
                            <button class="btn-primary btn pull-right"  id="pAdd" name="pAdd" onclick="location.href='/Bus/Add'">Дохилкунӣ</button>
                        </div>
                    </div>
                    <div class="card-body">

                        <table class="table table-hover my-0">
                            <thead>
                            <tr>
                                <th>#</th>
                                <th>Рақами автобус</th>
                                <th>Рақами хатсайр</th>
                                <th>Latitude</th>
                                <th>Longitude</th>
                                <th>Диганкуни</th>
                                <th>Хориҷкунӣ</th>
                            </tr>
                            </thead>
                            <tbody id="body">
                            @for (var i = 0; i < Model.Count; i++)
                            {
                                <tr class="clickable-row" data-href="#mymodal" id="row-@i "data-toggle="modal">
                                    <td>@(i + 1)</td>
                                    <td id="placeName-@i">@Model[i].BusNum</td>
                                    <td id="latitude-@i">@Model[i].RouteNum</td>
                                    <td >@Model[i].Latitude</td>
                                    <td id="longitude-@i">@Model[i].Longitude</td>
                                    <td><button type="button" class="btn btn-primary btn-sm" id="editBtn-@i" onclick="location.href='/Bus/View/'+'@Model[i].Id'">Дигаркунӣ</button>
                            
                                    <td><button type="button" class="btn btn-danger btn-sm" id="deleteBtn-@i" onclick="location.href='/Bus/Delete/'+'@Model[i].Id'">Хориҷкунӣ</button></td>

                                </tr>
                            }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</main>
<script>
document.getElementById("home").classList.remove("active");
document.getElementById("buses").classList.add("active");
</script>
</body>
